# inventory.yaml
# Example inventory file for Terraform
applications:
  cloudnova:
    resources:
      - kind: rds
        env: dev
      - kind: "null"
        env: dev
  happy:
    resources:
      - kind: ec2
        env: dev
  joy:
    resources:
      - kind: "null"
        env: dev